import { TemperatureDirective } from './temperature.directive';

describe('TemperatureDirective', () => {
  it('should create an instance', () => {
    const directive = new TemperatureDirective();
    expect(directive).toBeTruthy();
  });
});
